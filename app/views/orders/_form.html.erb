<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :naam %><br>
    <%= f.text_area :naam %>
  </div>
  <div class="field">
    <%= f.label :nummer %><br>
    <%= f.number_field :nummer %>
  </div>
  <div class="field">
    <%= f.label :prijs %><br>
    <%= f.text_field :prijs %>
  </div>
  <div class="field">
    <%= f.label :personen_id %><br>
    <%= f.text_field :personen_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
